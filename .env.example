# Base de datos MySQL (impaorg)
# Copiá este archivo a .env y completá DB_PASSWORD
DB_NAME=impaorg
DB_USER=joacoabe
DB_PASSWORD=tu_contraseña_aqui
DB_HOST=127.0.0.1
DB_PORT=3306

# Producción (imparg.org) - obligatorios con settings.production
# SECRET_KEY=generar-una-clave-secreta-larga-y-aleatoria
# ALLOWED_HOSTS=imparg.org,www.imparg.org
# WAGTAILADMIN_BASE_URL=https://imparg.org
# STATIC_URL=/impa-static/  (por defecto; necesario detrás del proxy)

# Intranet (para futura integración)
INTRANET_URL=https://imparg.org/intranet

# API pública de iglesias (para sync_churches_from_intranet)
# INTRANET_CHURCHES_API_URL=https://imparg.org/intranet/api/v1/public/churches
# INTRANET_CHURCHES_API_KEY=  # opcional, si la API exige X-API-Key
